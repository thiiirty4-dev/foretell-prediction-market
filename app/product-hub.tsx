"use client";

import { FormEvent, useState } from "react";
import { startRegistration } from "@simplewebauthn/browser";

type User = { id: string; email: string; displayName: string; isAdmin?: boolean; walletAddress?: string; bio?: string };
type Market = { id: string; title: string; category: string; yesPrice: number; closesAt: number; description: string };
type Panel = "detail" | "community" | "watchlist" | "rankings" | "alerts" | "profile" | "onchain" | "admin" | null;

export default function ProductHub({ user, market, onRequireAccount }: { user: User | null; market?: Market; onRequireAccount: () => void }) {
  const [panel, setPanel] = useState<Panel>(null); const [payload, setPayload] = useState<any>(null); const [busy, setBusy] = useState(false); const [message, setMessage] = useState(""); const [wallet, setWallet] = useState(user?.walletAddress ?? ""); const [chainId, setChainId] = useState("");

  async function open(next: Exclude<Panel, null>) {
    if (["watchlist", "alerts", "profile", "admin"].includes(next) && !user) { onRequireAccount(); return; }
    setPanel(next); setMessage(""); setPayload(null);
    const view = next === "community" ? "community" : next === "profile" ? "account" : next === "detail" ? "market&marketId=" + encodeURIComponent(market?.id ?? "") : next === "rankings" ? "leaderboard" : next === "alerts" ? "notifications" : next === "watchlist" ? "favorites" : next === "admin" ? "admin" : "";
    if (!view) return;
    setBusy(true);
    try {
      if (next === "detail") {
        const [detailResponse, terminalResponse] = await Promise.all([fetch("/api/product?view=" + view, { cache: "no-store" }), fetch("/api/market-terminal?marketId=" + encodeURIComponent(market?.id ?? ""), { cache: "no-store" })]);
        const [detail, terminal] = await Promise.all([detailResponse.json(), terminalResponse.json()]);
        if (!detailResponse.ok) throw new Error(detail.error); if (!terminalResponse.ok) throw new Error(terminal.error);
        setPayload({ ...detail, terminal });
      } else {
        const response = await fetch(next === "community" ? "/api/community?mode=latest" : next === "profile" ? "/api/account/center" : "/api/product?view=" + view, { cache: "no-store" });
        const result = await response.json(); if (!response.ok) throw new Error(result.error); setPayload(result);
      }
    }
    catch (error) { setMessage(error instanceof Error ? error.message : "Unable to load"); }
    finally { setBusy(false); }
  }

  async function action(body: Record<string, unknown>, refresh?: Panel) {
    if (!user) { onRequireAccount(); return; }
    setBusy(true); setMessage("");
    try { const response = await fetch("/api/product", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(body) }); const result = await response.json(); if (!response.ok) throw new Error(result.error); setMessage("Saved."); if (refresh) await open(refresh as Exclude<Panel, null>); else if (result.favorite !== undefined) setPayload((current: any) => ({ ...current, favorite: result.favorite })); }
    catch (error) { setMessage(error instanceof Error ? error.message : "Action failed"); }
    finally { setBusy(false); }
  }

  async function comment(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const form = new FormData(event.currentTarget); await action({ action: "comment", marketId: market?.id, body: form.get("body") }, "detail"); event.currentTarget.reset(); }
  async function report(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const form = new FormData(event.currentTarget); await action({ action: "report", marketId: market?.id, reason: form.get("reason") }); event.currentTarget.reset(); }
  async function priceAlert(event: FormEvent<HTMLFormElement>) { event.preventDefault(); if (!user) { onRequireAccount(); return; } const form = new FormData(event.currentTarget); setBusy(true); setMessage(""); try { const response = await fetch("/api/market-terminal", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ marketId: market?.id, direction: form.get("direction"), threshold: form.get("threshold") }) }); const result = await response.json(); if (!response.ok) throw new Error(result.error); await open("detail"); setMessage("Price alert saved."); } catch (error) { setMessage(error instanceof Error ? error.message : "Unable to save alert"); } finally { setBusy(false); } }
  async function deleteAlert(id: string) { setBusy(true); setMessage(""); try { const response = await fetch("/api/market-terminal", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ action: "delete-alert", id }) }); const result = await response.json(); if (!response.ok) throw new Error(result.error); await open("detail"); setMessage("Alert removed."); } catch (error) { setMessage(error instanceof Error ? error.message : "Unable to remove alert"); } finally { setBusy(false); } }
  async function shareMarket() { try { await navigator.clipboard.writeText(window.location.origin + "/?market=" + encodeURIComponent(market?.id ?? "")); setMessage("Market link copied."); } catch { setMessage("Copy was blocked. Use the address bar to share this page."); } }
  async function loadAccount() { const response = await fetch("/api/account/center", { cache: "no-store" }); const result = await response.json(); if (!response.ok) throw new Error(result.error); setPayload(result); }
  async function saveProfile(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const form = new FormData(event.currentTarget); await action({ action: "profile", bio: form.get("bio") }); await loadAccount(); }

  async function addPasskey() {
    if (!user) { onRequireAccount(); return; } setBusy(true); setMessage("");
    try { const optionsResponse = await fetch("/api/auth/passkey/register/options", { method: "POST" }); const options = await optionsResponse.json(); if (!optionsResponse.ok) throw new Error(options.error); const credential = await startRegistration({ optionsJSON: options }); const verifyResponse = await fetch("/api/auth/passkey/register/verify", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(credential) }); const result = await verifyResponse.json(); if (!verifyResponse.ok || !result.verified) throw new Error(result.error || "Passkey setup failed"); await loadAccount(); setMessage("Passkey added to your account."); }
    catch (error) { setMessage(error instanceof Error && error.name === "NotAllowedError" ? "Passkey request was cancelled or timed out." : error instanceof Error ? error.message : "Passkey setup failed"); }
    finally { setBusy(false); }
  }

  async function loadCommunity(mode: "latest" | "following") {
    if (mode === "following" && !user) { onRequireAccount(); return; }
    setBusy(true); setMessage("");
    try { const response = await fetch("/api/community?mode=" + mode, { cache: "no-store" }); const result = await response.json(); if (!response.ok) throw new Error(result.error); setPayload(result); }
    catch (error) { setMessage(error instanceof Error ? error.message : "Unable to load community"); }
    finally { setBusy(false); }
  }

  async function communityAction(body: Record<string, unknown>) {
    if (!user) { onRequireAccount(); return false; }
    setBusy(true); setMessage("");
    try { const response = await fetch("/api/community", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(body) }); const result = await response.json(); if (!response.ok) throw new Error(result.error); await loadCommunity(payload?.mode === "following" ? "following" : "latest"); return true; }
    catch (error) { setMessage(error instanceof Error ? error.message : "Community action failed"); return false; }
    finally { setBusy(false); }
  }

  async function publishSignal(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const form = event.currentTarget; const data = new FormData(form); if (await communityAction({ action: "publish", body: data.get("body"), marketId: data.get("linkMarket") ? market?.id : null })) form.reset(); }

  async function connect() {
    if (!user) { onRequireAccount(); return; }
    const provider = (window as unknown as { ethereum?: { request(args: { method: string; params?: unknown[] }): Promise<unknown> } }).ethereum;
    if (!provider) { setMessage("Install an EVM wallet such as MetaMask or Rabby first."); return; }
    try { const accounts = await provider.request({ method: "eth_requestAccounts" }) as string[]; const network = await provider.request({ method: "eth_chainId" }) as string; setWallet(accounts[0]); setChainId(network); await action({ action: "wallet", address: accounts[0] }); }
    catch (error) { setMessage(error instanceof Error ? error.message : "Wallet connection was cancelled"); }
  }

  async function switchTestnet() {
    const provider = (window as unknown as { ethereum?: { request(args: { method: string; params?: unknown[] }): Promise<unknown> } }).ethereum;
    if (!provider) { setMessage("No browser wallet detected."); return; }
    try { await provider.request({ method: "wallet_switchEthereumChain", params: [{ chainId: "0x279f" }] }); setChainId("0x279f"); setMessage("Wallet switched to Monad Testnet."); }
    catch { setMessage("Add Monad Testnet in your wallet, then try again. Chain ID: 10143."); }
  }

  return <>
    <section className="product-toolbar" aria-label="Product tools">
      <button onClick={() => open("detail")}>Market details</button><button className="community-tool" onClick={() => open("community")}><i /> Community</button><button onClick={() => open("watchlist")}>Watchlist</button><button onClick={() => open("rankings")}>Leaderboard</button><button onClick={() => open("alerts")}>Notifications</button><button onClick={() => open("profile")}>User center</button><button className="chain-tool" onClick={() => open("onchain")}><i /> Onchain lab</button>{user?.isAdmin ? <button onClick={() => open("admin")}>Admin</button> : null}
    </section>
    {panel ? <div className="modal-backdrop" onMouseDown={() => setPanel(null)}><section className="product-modal" onMouseDown={(event) => event.stopPropagation()}><div className="modal-head"><div><p className="eyebrow">FORETELL / {panel.toUpperCase()}</p><h2>{panelTitle(panel, market?.title)}</h2></div><button onClick={() => setPanel(null)}>×</button></div>{busy && !payload ? <div className="modal-empty">Loading…</div> : null}{message ? <p className="product-message">{message}</p> : null}
      {panel === "detail" && payload ? <MarketDetail market={market!} data={payload} user={user} busy={busy} onFavorite={() => action({ action: "favorite", marketId: market?.id })} onComment={comment} onReport={report} onAlert={priceAlert} onDeleteAlert={deleteAlert} onShare={shareMarket} /> : null}
      {panel === "community" && payload ? <Community data={payload} user={user} market={market} busy={busy} onMode={loadCommunity} onPublish={publishSignal} onAction={communityAction} onRequireAccount={onRequireAccount} /> : null}
      {panel === "watchlist" && payload ? <SimpleMarkets items={payload.favorites} /> : null}
      {panel === "rankings" && payload ? <Leaderboard items={payload.leaderboard} /> : null}
      {panel === "alerts" && payload ? <Notifications items={payload.notifications} /> : null}
      {panel === "profile" && payload ? <AccountCenter data={payload} busy={busy} wallet={wallet} onSave={saveProfile} onPasskey={addPasskey} onWallet={connect} /> : null}
      {panel === "onchain" ? <div className="chain-lab"><div className="chain-status"><span><small>NETWORK</small><b>Monad Testnet</b></span><span><small>CHAIN ID</small><b>10143</b></span><span><small>CONTRACT</small><b>Pre-audit</b></span></div><p>The onchain module is isolated from play-money markets. Wallet connection is live; collateral trading remains disabled until the testnet contract is deployed and reviewed.</p><div className="wallet-card"><small>CONNECTED WALLET</small><b>{wallet ? wallet.slice(0, 8) + "…" + wallet.slice(-6) : "Not connected"}</b><span>{chainId ? chainId === "0x279f" ? "Monad Testnet" : "Wrong network: " + chainId : "Connect an EVM wallet to begin"}</span></div><div className="chain-actions"><button onClick={connect}>Connect wallet</button><button onClick={switchTestnet}>Switch to testnet</button></div><p className="chain-warning">No deposits, withdrawals, or real-money transactions are enabled.</p></div> : null}
      {panel === "admin" && payload ? <Admin data={payload} busy={busy} action={action} /> : null}
    </section></div> : null}
  </>;
}

function MarketDetail({ market, data, user, busy, onFavorite, onComment, onReport, onAlert, onDeleteAlert, onShare }: any) {
  const points = data.history?.length ? data.history : [{ yesPrice: market.yesPrice, createdAt: Date.now() }]; const polyline = points.map((point: any, index: number) => `${points.length === 1 ? 50 : index * 100 / (points.length - 1)},${100 - point.yesPrice}`).join(" ");
  return <div className="market-detail"><div className="detail-grid"><div><span className="tag">{market.category}</span><p>{market.description}</p><dl><div><dt>Close date</dt><dd>{new Date(market.closesAt).toLocaleDateString()}</dd></div><div><dt>Resolution source</dt><dd>{data.governance?.resolutionSource ?? "Publicly verifiable sources"}</dd></div><div><dt>Status</dt><dd>{data.governance?.outcome ? "Resolved " + data.governance.outcome : "Open"}</dd></div></dl></div><div className="history-chart"><div><span>Probability history</span><b>{market.yesPrice}% YES</b></div><svg viewBox="0 0 100 100" preserveAspectRatio="none"><polyline points={polyline} /></svg><small>{points.length} recorded price points</small></div></div><MarketTerminal market={market} data={data.terminal} user={user} busy={busy} onAlert={onAlert} onDeleteAlert={onDeleteAlert} onShare={onShare} /><button className={data.favorite ? "saved-button active" : "saved-button"} disabled={busy} onClick={onFavorite}>{data.favorite ? "★ Saved to watchlist" : "☆ Save to watchlist"}</button><div className="community"><h3>Discussion <span>{data.comments?.length ?? 0}</span></h3>{user ? <form onSubmit={onComment}><textarea name="body" required minLength={2} maxLength={500} placeholder="Share evidence or your reasoning…" /><button disabled={busy}>Post comment</button></form> : <p>Log in to join the discussion.</p>}<div className="comment-list">{data.comments?.map((item: any) => <article key={item.id}><span>{item.displayName.slice(0, 2).toUpperCase()}</span><div><b>{item.displayName}{item.role === "admin" ? <small> MOD</small> : null}</b><p>{item.body}</p><time>{new Date(item.createdAt).toLocaleString()}</time></div></article>)}</div>{user ? <details className="report-box"><summary>Report this market</summary><form onSubmit={onReport}><input name="reason" required minLength={5} maxLength={300} placeholder="Explain the issue" /><button>Submit report</button></form></details> : null}</div></div>;
}

function MarketTerminal({ market, data, user, busy, onAlert, onDeleteAlert, onShare }: any) {
  const maxDepth = Math.max(...(data?.book?.bids ?? []).map((item: any) => item.size), ...(data?.book?.asks ?? []).map((item: any) => item.size), 1);
  return <section className="market-terminal"><div className="terminal-heading"><div><span className="eyebrow">MARKET TERMINAL</span><h3>Depth, holders and alerts.</h3></div><div><span className="terminal-badge">SIMULATED DEPTH</span><button className="share-market" onClick={onShare}>Copy market link</button></div></div><div className="terminal-grid"><section className="terminal-card"><header><b>YES order book</b><span>{data?.spread ?? 0}¢ spread</span></header><div className="depth-head"><span>BID PRICE / SIZE</span><span>ASK PRICE / SIZE</span></div>{(data?.book?.bids ?? []).map((bid: any, index: number) => { const ask = data.book.asks[index]; return <div className="depth-row" key={bid.price} style={{ "--depth": Math.round(Math.max(bid.size, ask?.size ?? 0) / maxDepth * 50) + "%" } as any}><span className="bid"><b>{bid.price}¢ BID</b> · {formatMoney(bid.size)}</span><span className="ask"><b>{ask?.price}¢ ASK</b> · {formatMoney(ask?.size ?? 0)}</span></div>; })}<div className="depth-mid"><span>MID {data?.book?.midpoint}¢</span><span>SPREAD {data?.spread}¢</span></div></section><section className="terminal-card"><header><b>Top holders</b><span>by simulated cost</span></header><div className="holder-list">{data?.holders?.length ? data.holders.map((holder: any, index: number) => <div className="holder-row" key={holder.id}><span>#{index + 1}</span><div><b>{holder.name}</b><small>{holder.yesShares.toFixed(1)} YES · {holder.noShares.toFixed(1)} NO</small></div><strong>{formatMoney(holder.invested)}</strong></div>) : <div className="modal-empty">No holders yet.</div>}</div></section><section className="terminal-card"><header><b>Recent trades</b><span>latest 20 fills</span></header><div className="terminal-trades">{data?.trades?.length ? data.trades.slice(0, 8).map((trade: any) => <div className="terminal-trade" key={trade.id}><span className={trade.side === "YES" ? "yes" : "no"}>{trade.side}</span><b>{trade.trader}</b><span>{trade.shares.toFixed(1)} @ {trade.price}¢</span><time>{relativeTime(trade.createdAt)}</time></div>) : <div className="modal-empty">No trades recorded.</div>}</div></section><section className="terminal-card alert-card"><header><b>Probability alerts</b><span>checked after every fill</span></header>{user ? <form className="alert-form" onSubmit={onAlert}><select name="direction" defaultValue="above"><option value="above">YES rises above</option><option value="below">YES falls below</option></select><input name="threshold" type="number" min="1" max="99" required defaultValue={market.yesPrice} aria-label="Alert probability" /><button disabled={busy}>Set alert</button></form> : <p className="alert-login-note">Log in to save a probability alert.</p>}<div className="alert-list">{data?.alerts?.map((alert: any) => <article key={alert.id}><b>YES {alert.direction} {alert.threshold}%</b><span className={alert.triggeredAt ? "triggered" : ""}>{alert.triggeredAt ? "TRIGGERED" : alert.active ? "ACTIVE" : "ENDED"}</span><button disabled={busy} onClick={() => onDeleteAlert(alert.id)}>Remove</button></article>)}</div></section><section className="terminal-card"><header><b>Related markets</b><span>{market.category}</span></header><div className="related-list">{data?.related?.length ? data.related.map((item: any) => <a href={"/?market=" + encodeURIComponent(item.id)} key={item.id}><b>{item.title}</b><strong>{item.yesPrice}%</strong><small>{formatMoney(item.volume / 100)} volume</small></a>) : <div className="modal-empty">No related markets yet.</div>}</div></section></div></section>;
}
function SimpleMarkets({ items }: any) { return <div className="simple-list">{items?.length ? items.map((item: any) => <article key={item.id}><div><small>{item.category}</small><b>{item.title}</b></div><strong>{item.yesPrice}%</strong></article>) : <div className="modal-empty">Your watchlist is empty.</div>}</div>; }
function Leaderboard({ items }: any) { return <div className="leaderboard">{items?.map((item: any, index: number) => <article key={item.id}><span>{String(index + 1).padStart(2, "0")}</span><div><b>{item.displayName}</b><small>{item.trades} trades</small></div><strong>{formatMoney(item.volumeCents / 100)} volume</strong></article>)}</div>; }
function Notifications({ items }: any) { return <div className="notification-list">{items?.length ? items.map((item: any) => <article className={item.isRead ? "" : "unread"} key={item.id}><i /><div><b>{item.title}</b><p>{item.body}</p><time>{new Date(item.createdAt).toLocaleString()}</time></div></article>) : <div className="modal-empty">No notifications yet.</div>}</div>; }
function AccountCenter({ data, busy, wallet, onSave, onPasskey, onWallet }: any) {
  const stats = data.stats ?? {}; const user = data.user;
  return <div className="account-center"><section className="account-identity"><div className="account-avatar">{user.displayName.slice(0, 2).toUpperCase()}</div><div><span>{user.isAdmin ? "OPERATOR ACCOUNT" : "FORETELL MEMBER"}</span><h3>{user.displayName}</h3><p>{user.email}</p><small>Member since {new Date(user.createdAt).toLocaleDateString()}</small></div><div className="completion-ring" style={{ "--completion": data.completion + "%" } as any}><b>{data.completion}%</b><span>SETUP</span></div></section><section className="account-stats"><article><small>PLAY BALANCE</small><b>{formatMoney(stats.balance)}</b></article><article><small>ORDERS</small><b>{stats.orders}</b></article><article><small>MARKETS</small><b>{stats.markets}</b></article><article><small>COMMUNITY</small><b>{stats.posts} posts</b></article></section><div className="account-grid"><section className="account-block"><header><div><span className="eyebrow">PROFILE</span><h3>Public identity</h3></div></header><form className="profile-form" onSubmit={onSave}><label>Bio<textarea name="bio" maxLength={240} defaultValue={user.bio} placeholder="What markets and topics do you follow?" /></label><button className="trade-submit" disabled={busy}>{busy ? "Saving…" : "Save profile"}</button></form></section><section className="account-block"><header><div><span className="eyebrow">SECURITY</span><h3>Account access</h3></div></header><div className="security-list"><span><i className={stats.passkeys ? "ready" : ""} /><div><b>Passkey</b><small>{stats.passkeys ? stats.passkeys + " registered" : "Not configured"}</small></div><button disabled={busy} onClick={onPasskey}>{stats.passkeys ? "Add another" : "Set up"}</button></span><span><i className={wallet || user.walletAddress ? "ready" : ""} /><div><b>Wallet</b><small>{wallet || user.walletAddress ? shortWallet(wallet || user.walletAddress) : "Not connected"}</small></div><button disabled={busy} onClick={onWallet}>{wallet || user.walletAddress ? "Reconnect" : "Connect"}</button></span></div></section></div><section className="account-block recent-orders"><header><div><span className="eyebrow">ACTIVITY</span><h3>Recent orders</h3></div><span>{formatMoney(stats.spent)} total placed</span></header>{data.recentTrades?.length ? data.recentTrades.map((trade: any) => <article key={trade.id}><span className={trade.side === "YES" ? "yes-order" : "no-order"}>{trade.side}</span><div><b>{trade.marketTitle}</b><small>{trade.shares.toFixed(2)} shares at {trade.price}¢</small></div><strong>{formatMoney(trade.amount)}</strong><time>{new Date(trade.createdAt).toLocaleDateString()}</time></article>) : <div className="modal-empty">Your completed orders will appear here.</div>}</section></div>;
}
function Community({ data, user, market, busy, onMode, onPublish, onAction, onRequireAccount }: any) {
  return <div className="community-signals"><form className="signal-composer" onSubmit={onPublish}><span className="signal-avatar">{user ? user.displayName.slice(0, 2).toUpperCase() : "YOU"}</span><div><textarea name="body" required minLength={3} maxLength={400} placeholder={user ? "Share evidence, a forecast, or a counterpoint…" : "Log in to publish a market signal."} onFocus={() => !user && onRequireAccount()} /><footer><label><input type="checkbox" name="linkMarket" defaultChecked />Link: {market?.title ?? "current market"}</label><span>400 max</span><button disabled={busy || !user}>{busy ? "Publishing…" : "Publish signal"}</button></footer></div></form><div className="signal-tabs"><button className={data.mode === "latest" ? "active" : ""} onClick={() => onMode("latest")}>Latest</button><button className={data.mode === "following" ? "active" : ""} onClick={() => onMode("following")}>Following</button></div><div className="signal-feed">{data.posts?.length ? data.posts.map((post: any) => <article className="signal-post" key={post.id}><span className="signal-avatar">{post.initials}</span><div><header><span><b>{post.authorName}</b><small>{relativeTime(post.createdAt)}</small></span>{!post.viewerIsAuthor ? <button className={post.following ? "following" : ""} onClick={() => onAction({ action: "follow", profileId: post.authorId, active: !post.following })}>{post.following ? "Following" : "+ Follow"}</button> : <button className="delete-signal" onClick={() => onAction({ action: "delete", postId: post.id })}>Delete</button>}</header><p>{post.body}</p>{post.marketTitle ? <span className="linked-market">MARKET · {post.marketTitle}</span> : null}<footer><button className={post.liked ? "liked" : ""} onClick={() => onAction({ action: "like", postId: post.id, active: !post.liked })}>{post.liked ? "♥" : "♡"} {post.likeCount}</button><span>{post.followerCount} followers</span><time>{new Date(post.createdAt).toLocaleString()}</time></footer></div></article>) : <div className="modal-empty"><b>{data.mode === "following" ? "No signals from people you follow." : "No community signals yet."}</b><span>{data.mode === "following" ? "Follow an analyst from the latest feed." : "Be the first to publish a clear, evidence-based view."}</span></div>}</div></div>;
}
function Admin({ data, busy, action }: any) { return <div className="admin-panel"><div className="admin-metrics">{Object.entries(data.metrics ?? {}).map(([key, value]) => <span key={key}><small>{key.replace(/([A-Z])/g, " $1")}</small><b>{String(value)}</b></span>)}</div><h3>Open reports</h3><div className="admin-reports">{data.reports?.length ? data.reports.map((report: any) => <article key={report.id}><div><b>{report.title}</b><p>{report.reason}</p><small>Reported by {report.reporter}</small></div><button disabled={busy} onClick={() => action({ action: "dismiss-report", reportId: report.id }, "admin")}>Dismiss</button></article>) : <p>No open reports.</p>}</div><h3>Resolve markets</h3><div className="admin-markets">{data.markets?.filter((market: any) => market.status === "open").map((market: any) => <article key={market.id}><span>{market.title}</span><div><button disabled={busy} onClick={() => action({ action: "resolve", marketId: market.id, outcome: "YES" }, "admin")}>Resolve YES</button><button disabled={busy} onClick={() => action({ action: "resolve", marketId: market.id, outcome: "NO" }, "admin")}>Resolve NO</button></div></article>)}</div></div>; }
function panelTitle(panel: Panel, market?: string) { return panel === "detail" ? market ?? "Market details" : ({ community: "Community signals.", watchlist: "Markets you are following.", rankings: "Community leaderboard.", alerts: "Your notifications.", profile: "Public profile.", onchain: "Onchain test environment.", admin: "Operations console." } as Record<string, string>)[panel ?? ""] ?? ""; }
function formatMoney(value: number) { return new Intl.NumberFormat("en-US", { style: "currency", currency: "USD", notation: "compact", maximumFractionDigits: 1 }).format(value); }
function relativeTime(timestamp: number) { const seconds = Math.max(0, Math.floor((Date.now() - timestamp) / 1000)); if (seconds < 60) return "now"; const minutes = Math.floor(seconds / 60); if (minutes < 60) return minutes + "m"; const hours = Math.floor(minutes / 60); if (hours < 24) return hours + "h"; return Math.floor(hours / 24) + "d"; }
function shortWallet(address: string) { return address.slice(0, 7) + "…" + address.slice(-5); }


